$(document).ready(function() {
    function t() {
        var t = $(window).width(), i = $(window).height();
        $("#width").text(t), $("#height").text(i);
    }
    $(window).scroll(function() {
        $(document).scrollTop() > 400 ? $(".up").stop().addClass("show") : $(".up").stop().removeClass("show");
    }), $("a[href*=#]:not([href=#])").click(function() {
        if (location.pathname.replace(/^\//, "") === this.pathname.replace(/^\//, "") || location.hostname === this.hostname) {
            var t = $(this.hash);
            if ((t = t.length ? t : $("[name=" + this.hash.slice(1) + "]")).length) return $("html,body").animate({
                scrollTop: t.offset().top
            }, 1e3), !1;
        }
    }), $(window).resize(function() {
        t();
    }), $(".tabs li a").click(function() {
        var t = $(this).attr("data");
        $(".tabs li a").removeClass("active"), $(this).addClass("active"), $("#" + t).show().siblings("div").hide();
    }), $.fn.equalHeight = function() {
        return this.height(Math.max.apply(this, $.map(this, function(t) {
            return $(t).height();
        })));
    }, $(".equal").equalHeight();
    var i = $(".include");
    jQuery.each(i, function() {
        var t = "/" + $(this).data("include") + ".html";
        $(this).load(t);
    }), $("input, textarea").on("propertychange keyup input paste", function() {
        var t = $(this).data("limit"), i = t - $(this).val().length;
        i <= 0 ? ($(this).val($(this).val().substring(0, t)), $(this).nextAll("div").css("color", "#cc6666")) : $(this).nextAll("div").css("color", ""), 
        $(this).nextAll("div").text((i <= 0 ? 0 : i) + " characters remaining.");
    }), $(".inactive").click(function(t) {
        return t.preventDefault(), !1;
    });
});
//# sourceMappingURL=script.min.js.map